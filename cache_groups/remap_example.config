# Apache Traffic Server remap.config example for Cache Groups plugin
# 
# Add these lines to your remap.config file to enable the Cache Groups plugin
# for specific mappings or globally.

# Example 1: Enable cache groups for a specific domain
map http://example.com/ http://backend.example.com/ @plugin=lua.so @pparam=/opt/trafficserver/etc/trafficserver/lua/cache_groups.lua

# Example 2: Enable cache groups for HTTPS traffic
map https://example.com/ https://backend.example.com/ @plugin=lua.so @pparam=/opt/trafficserver/etc/trafficserver/lua/cache_groups.lua

# Example 3: Enable cache groups globally for all HTTP traffic
# (Add this to your global remap rules)
map / @plugin=lua.so @pparam=/opt/trafficserver/etc/trafficserver/lua/cache_groups.lua

# Note: Make sure the lua.so plugin is available in your ATS installation
# and the cache_groups.lua file is placed in the correct location:
# /opt/trafficserver/etc/trafficserver/lua/cache_groups.lua
#
# Configuration is now embedded in the Lua script itself - no separate config file needed